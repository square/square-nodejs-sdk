/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Square from "../index.js";

/**
 * Represents a refund processed for a Square transaction.
 */
export interface Refund {
    /** The refund's unique ID. */
    id: string;
    /** The ID of the refund's associated location. */
    location_id: string;
    /** The ID of the transaction that the refunded tender is part of. */
    transaction_id?: string | null;
    /** The ID of the refunded tender. */
    tender_id?: string | null;
    /** The timestamp for when the refund was created, in RFC 3339 format. */
    created_at?: string;
    /** The reason for the refund being issued. */
    reason: string;
    /** The amount of money refunded to the buyer. */
    amount_money: Square.Money;
    /**
     * The current status of the refund (`PENDING`, `APPROVED`, `REJECTED`,
     * or `FAILED`).
     * See [RefundStatus](#type-refundstatus) for possible values
     */
    status: Square.RefundStatus;
    /** The amount of Square processing fee money refunded to the *merchant*. */
    processing_fee_money?: Square.Money;
    /**
     * Additional recipients (other than the merchant) receiving a portion of this refund.
     * For example, fees assessed on a refund of a purchase by a third party integration.
     */
    additional_recipients?: Square.AdditionalRecipient[] | null;
}
