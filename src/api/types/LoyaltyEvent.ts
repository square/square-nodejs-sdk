/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Square from "../index.js";

/**
 * Provides information about a loyalty event.
 * For more information, see [Search for Balance-Changing Loyalty Events](https://developer.squareup.com/docs/loyalty-api/loyalty-events).
 */
export interface LoyaltyEvent {
    /** The Square-assigned ID of the loyalty event. */
    id?: string;
    /**
     * The type of the loyalty event.
     * See [LoyaltyEventType](#type-loyaltyeventtype) for possible values
     */
    type: Square.LoyaltyEventType;
    /** The timestamp when the event was created, in RFC 3339 format. */
    created_at?: string;
    /** Provides metadata when the event `type` is `ACCUMULATE_POINTS`. */
    accumulate_points?: Square.LoyaltyEventAccumulatePoints;
    /** Provides metadata when the event `type` is `CREATE_REWARD`. */
    create_reward?: Square.LoyaltyEventCreateReward;
    /** Provides metadata when the event `type` is `REDEEM_REWARD`. */
    redeem_reward?: Square.LoyaltyEventRedeemReward;
    /** Provides metadata when the event `type` is `DELETE_REWARD`. */
    delete_reward?: Square.LoyaltyEventDeleteReward;
    /** Provides metadata when the event `type` is `ADJUST_POINTS`. */
    adjust_points?: Square.LoyaltyEventAdjustPoints;
    /** The ID of the [loyalty account](entity:LoyaltyAccount) associated with the event. */
    loyalty_account_id?: string;
    /** The ID of the [location](entity:Location) where the event occurred. */
    location_id?: string;
    /**
     * Defines whether the event was generated by the Square Point of Sale.
     * See [LoyaltyEventSource](#type-loyaltyeventsource) for possible values
     */
    source: Square.LoyaltyEventSource;
    /** Provides metadata when the event `type` is `EXPIRE_POINTS`. */
    expire_points?: Square.LoyaltyEventExpirePoints;
    /** Provides metadata when the event `type` is `OTHER`. */
    other_event?: Square.LoyaltyEventOther;
    /** Provides metadata when the event `type` is `ACCUMULATE_PROMOTION_POINTS`. */
    accumulate_promotion_points?: Square.LoyaltyEventAccumulatePromotionPoints;
}
