/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Square from "../index.js";

/**
 * V1Order
 */
export interface V1Order {
    /** Any errors that occurred during the request. */
    errors?: Square.Error_[] | null;
    /** The order's unique identifier. */
    id?: string;
    /** The email address of the order's buyer. */
    buyer_email?: string | null;
    /** The name of the order's buyer. */
    recipient_name?: string | null;
    /** The phone number to use for the order's delivery. */
    recipient_phone_number?: string | null;
    /**
     * Whether the tax is an ADDITIVE tax or an INCLUSIVE tax.
     * See [V1OrderState](#type-v1orderstate) for possible values
     */
    state?: Square.V1OrderState;
    /** The address to ship the order to. */
    shipping_address?: Square.Address;
    /** The amount of all items purchased in the order, before taxes and shipping. */
    subtotal_money?: Square.V1Money;
    /** The shipping cost for the order. */
    total_shipping_money?: Square.V1Money;
    /** The total of all taxes applied to the order. */
    total_tax_money?: Square.V1Money;
    /** The total cost of the order. */
    total_price_money?: Square.V1Money;
    /** The total of all discounts applied to the order. */
    total_discount_money?: Square.V1Money;
    /** The time when the order was created, in ISO 8601 format. */
    created_at?: string;
    /** The time when the order was last modified, in ISO 8601 format. */
    updated_at?: string;
    /** The time when the order expires if no action is taken, in ISO 8601 format. */
    expires_at?: string | null;
    /** The unique identifier of the payment associated with the order. */
    payment_id?: string | null;
    /** A note provided by the buyer when the order was created, if any. */
    buyer_note?: string | null;
    /** A note provided by the merchant when the order's state was set to COMPLETED, if any */
    completed_note?: string | null;
    /** A note provided by the merchant when the order's state was set to REFUNDED, if any. */
    refunded_note?: string | null;
    /** A note provided by the merchant when the order's state was set to CANCELED, if any. */
    canceled_note?: string | null;
    /** The tender used to pay for the order. */
    tender?: Square.V1Tender;
    /** The history of actions associated with the order. */
    order_history?: Square.V1OrderHistoryEntry[] | null;
    /** The promo code provided by the buyer, if any. */
    promo_code?: string | null;
    /** For Bitcoin transactions, the address that the buyer sent Bitcoin to. */
    btc_receive_address?: string | null;
    /** For Bitcoin transactions, the price of the buyer's order in satoshi (100 million satoshi equals 1 BTC). */
    btc_price_satoshi?: number | null;
}
